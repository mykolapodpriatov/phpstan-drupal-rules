{
    "name": "your-org/phpstan-drupal-rules",
    "description": "Opinionated PHPStan rules for Drupal 10.3+/11 codebases — DI hygiene, hook signature checks, deprecated API detection and entity query access checks.",
    "type": "phpstan-extension",
    "keywords": [
        "drupal",
        "phpstan",
        "static-analysis",
        "dependency-injection",
        "drupal-10",
        "drupal-11"
    ],
    "license": "MIT",
    "authors": [
        {
            "name": "Your Org Engineering",
            "email": "engineering@example.com"
        }
    ],
    "require": {
        "php": "^8.3",
        "phpstan/phpstan": "^1.11 || ^2.0",
        "mglaman/phpstan-drupal": "^1.3 || ^2.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5 || ^11.0",
        "phpstan/phpstan-strict-rules": "^1.6 || ^2.0"
    },
    "autoload": {
        "psr-4": {
            "YourOrg\\PhpStanDrupalRules\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "YourOrg\\PhpStanDrupalRules\\Tests\\": "tests/"
        }
    },
    "extra": {
        "phpstan": {
            "includes": [
                "extension.neon"
            ]
        }
    },
    "scripts": {
        "test": "phpunit",
        "analyze": "phpstan analyse --memory-limit=512M",
        "ci": [
            "@analyze",
            "@test"
        ]
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "phpstan/extension-installer": true
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
